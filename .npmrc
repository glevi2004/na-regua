# Metro (Expo) nao resolve o store simbolico do pnpm — sem isto o app mobile
# quebra ao importar qualquer pacote do workspace.
node-linker=hoisted

# Falha se o lockfile estiver desatualizado em relacao aos package.json (CI).
strict-peer-dependencies=false
auto-install-peers=true
